{
  "name": "bookmarkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "dexie": "^4.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.306",
    "typescript": "^5.8.3",
    "vite": "^6.1.0",
    "vitest": "^5.0.0"
  }
}
